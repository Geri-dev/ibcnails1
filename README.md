# IBC Nails – Next.js

1. **Install:** `npm install`
2. **Run:** `npm run dev` → open http://localhost:3000
3. **Media:** Put images in `public/images/` and videos in `public/videos/` (see `public/README.md` for exact filenames).
